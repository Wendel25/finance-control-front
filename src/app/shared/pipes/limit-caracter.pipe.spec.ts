import { LimitCaracterPipe } from './limit-caracter.pipe';

describe('LimitCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
